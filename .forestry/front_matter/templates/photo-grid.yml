---
label: Photo Grid
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: sub_heading
  type: text
  config:
    required: false
  label: Sub Heading
  description: Client name, author name, etc.
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
